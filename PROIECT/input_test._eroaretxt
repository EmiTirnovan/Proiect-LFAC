class Person {
    int age;
    string name;

    int getAge() {
        return age;
    }
};

int add(int a, int b,int a, string b) {
    return a + b;
}

float y = 15;
bool wow=true;

main {
    p.age = 20;
    print(p.age);
    add(p.age, p.age);
    if (p.age > 18) {
        print(p.age);
    }
    if (wow == true)
    {
        wow=false;
    }
    while (p.age < 25) {
        p.age = p.age + 1;
    }
}
